# 결과

Pi 코딩 에이전트를 공식 npm 패키지 `@earendil-works/pi-coding-agent`로 전역 설치했다.

- 설치 버전: `0.85.1`
- 전역 설치 위치: `C:\Users\lollo\AppData\Roaming\npm`
- 검증: `pi --version`과 `pi --help`가 정상 종료했다.
- 셸 환경: Git Bash가 `D:\Program\Git\bin\bash.exe`에 존재한다.

남은 위험은 없다. 모델을 사용하려면 다음 실행 시 `pi`를 열고 `/login`으로 제공자 인증을 진행하면 된다.
