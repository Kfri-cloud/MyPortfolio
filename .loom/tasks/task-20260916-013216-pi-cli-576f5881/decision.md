# 결정

사용자가 지정한 `https://pi.dev/`의 공식 Quickstart를 기준으로 `npm install -g --ignore-scripts @earendil-works/pi-coding-agent`를 사용했다. `--ignore-scripts`는 Pi의 정상 설치에 필요하지 않은 의존성 lifecycle script 실행을 막는다.

모델 제공자 로그인과 API 키 등록은 Task 범위 밖이므로 수행하지 않았다.
